# Community Cloud may use Python 3.14 + uv first: every pin below must have matching
# manylinux/macOS wheels (no cp314 wheel => uv fails; pandas sdist => 45min+ pip build).
streamlit==1.57.0
pandas==2.3.3
openpyxl==3.1.5
ortools==9.15.6755
